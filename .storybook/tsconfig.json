{
  // This compiler option is important. DI doesn't work with Angular 8 otherwise!
  "compilerOptions": {
    "emitDecoratorMetadata": true
  },
  "extends": "../tsconfig.json",
  "exclude": [
    "../src/test.ts",
    "../src/**/*.spec.ts",
    "../projects/**/*.spec.ts"
  ],
  "include": [
    "../src/**/*",
    "../projects/**/*"
  ]
}
